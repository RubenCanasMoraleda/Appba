import 'package:flutter/material.dart';

class ApbaStyles {
  ApbaStyles._();
  //Border Radious
  static const BorderRadius borderRadiusS =
      BorderRadius.all(Radius.circular(2));
  static const BorderRadius borderRadiusM =
      BorderRadius.all(Radius.circular(8));
  static const BorderRadius borderRadiusL =
      BorderRadius.all(Radius.circular(12));
  static const BorderRadius borderRadiusFull =
      BorderRadius.all(Radius.circular(100));

  //Stroke
  static const double strokeS = 0.5;
  static const double strokeM = 1.5;
  static const double strokeL = 2;
  static const double strokeFocus = 4;
}
